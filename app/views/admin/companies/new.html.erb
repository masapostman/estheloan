<% @title = '加盟店の新規登録' %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_for @company ,url: admin_companies_path do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '登録' %>
      <%= link_to 'キャンセル', :admin_companies %>
    </div>
  <% end %>
</div>
